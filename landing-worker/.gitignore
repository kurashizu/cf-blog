node_modules
dist
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.vercel
.output
.netlify
.wrangler

# test + metrics output (npm run test:coverage / test:mutation)
/coverage
/reports
/.stryker-tmp
/.shots
/test-results

# Scratch probes: exploratory test files, never committed. They run in the
# local suite otherwise, so `npm test` and CI disagree about what ran.
tests/**/_scratch*/
